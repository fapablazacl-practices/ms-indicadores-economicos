
const assert = require('assert');
const mockery = require('mockery');

describe('IndicatorController', () => {
    describe('get', () => {
        it('should return the data from the service formatted', (done) => {
            done();
        });
    });
});
